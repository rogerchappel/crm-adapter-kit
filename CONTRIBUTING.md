# Contributing

Thank you for helping improve `crm-adapter-kit`.

## Local Checks

Use Node.js 20 or newer and run the release check before opening a pull request:

```sh
npm install
npm run release:check
```

Keep examples local-first and deterministic. Do not add fixtures that include real customer records, credentials, or live CRM endpoints.
