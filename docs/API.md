# API

- `loadCrmFixture(path)`: load contacts, accounts, and deals from JSON.
- `listContacts(data, query)`: filter contacts locally.
- `planTask(data, contactName, followUp)`: produce a dry-run task action.
- `summarizeCrm(data)`: count fixture records.
